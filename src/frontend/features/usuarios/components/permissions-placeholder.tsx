import { Alert } from "@mui/material";

export function PermissionsPlaceholder() {
  return (
    <Alert severity="info" sx={{ mt: 2 }}>
      Permissoes granulares serao implementadas na SPEC 006.
    </Alert>
  );
}
